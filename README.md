# elevenbeans.me

Personal profile page. Built with vanilla HTML, CSS, JS — zero dependencies.

## Features

- Work timeline & project showcase
- Dark/light theme with system preference detection
- i18n (EN / 中文)
- Hidden retro terminal (双击 / Ctrl+`)
- Hidden AI assistant overlay (hint button, `ai` in terminal, or Ctrl+Shift+`) — streams real replies from the NAS `profile-chat` endpoint, with localStorage memory and offline easter-egg commands
- Keyboard accessible (skip link, focus management, ARIA)
- Respects `prefers-reduced-motion`; works without JavaScript

## Deploy

Push to `main` — GitHub Pages auto-deploys to elevenbeans.me.
